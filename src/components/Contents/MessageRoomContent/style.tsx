import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { flex: 1 },
  flatList: { flex: 1, paddingHorizontal: 20 },
  containerFlatList: { paddingVertical: 30 },
});

export default styles;
